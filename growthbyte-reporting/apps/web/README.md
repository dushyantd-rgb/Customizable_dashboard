# Web service

Next.js 15 and React 18 application shell for the GrowthByte Reporting Platform.

Phase 1 contains only the home page, health page, and API health display. It does not contain dashboards, authentication, integration setup, or reporting features.

Run from the repository root with `pnpm --filter @growthbyte/web dev`.
