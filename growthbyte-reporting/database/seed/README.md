# Seed workflow

Only synthetic, non-production seed data may be stored here. Phase 1 intentionally has no seed SQL because no business-domain tables exist. Never copy client data, knowledge content, credentials, or production exports into this directory.
